class FormData{
  var name;
  var icon;
  int index;
  FormData(this.name, this.icon, this.index);

  @override
  String toString() {
    return 'FormData{name: $name, icon: $icon, index: $index}';
  }

}